spring:
  datasource:
    url: jdbc:h2:mem:test;
    username: root
    password: Funraiser1
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  liquibase:
    enabled: false